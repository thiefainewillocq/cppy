/*
    No braces, no semicolons. Indentation is the block structure.

    Plain int, and no -Hlibcppy/types.hpp on the command line: cppy has no
    types of its own, so libcppy is entirely optional.
*/

int main()
        printf("Hello world!\n")
        return 0
